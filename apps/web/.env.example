# Used server-side (Server Components, generateMetadata, sitemap) to call the
# API directly. Must be reachable from wherever the Next.js server runs.
API_INTERNAL_URL=http://localhost:4000

# Used client-side and for canonical/OG URLs. Must be the public site URL.
NEXT_PUBLIC_SITE_URL=http://localhost:3000
